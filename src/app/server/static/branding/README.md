Blink branding assets for dashboard/report pages.

Place files here:

- `logo/`: primary Blink logos (for example: `blink-logo-512.png`, `blink-logo.svg`)
- `favicon/`: favicon-generator outputs (`favicon.ico`, `apple-touch-icon.png`, `site.webmanifest`, png sizes, etc.)
